Import "main.py"

def follow_path(text):
    if text == "red":
        color = (0,0,0)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    elif text =="green":
        color = (0,0,0)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    else:
        color = (0,0,0)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()

    return 0

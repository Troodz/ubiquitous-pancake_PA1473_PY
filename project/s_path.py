import "main.py"
#black ==(0,0,0)
def follow_path(text):
    if text == "red":
        color = (102,0,0)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    elif text =="green":
        color = (0,102,0)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    elif text =="blue":
        color = (1,0,102)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    elif text =="poo_green":
        color = (101,102,0)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    elif text =="purple":
        color = (102,0,102)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    elif text =="brown":
        color = (91,43,83)
        color_now = color_sensor.measure()
        while color_now != color 
            go_robot_go()
        go_to_color()
    return 0
